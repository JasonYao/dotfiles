{
    "version": "alpha",
    "schema": "core",
    "actions": [
        {
            "action": "symlink",
            "files": {
                "config": "~/.ssh/config",
                "authorized_keys": "~/.ssh/authorized_keys"
            }
        }
    ],
    "profile": "personal"
}
