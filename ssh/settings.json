{
    "version": "alpha",
    "schema": "core",
    "actions": [
        {
            "action": "script",
            "files": {
                "setup_ssh_directory": "_"
            }
        },
        {
            "action": "symlink",
            "files": {
                "config.symlink": "~/.ssh/config",
                "authorized_keys.symlink": "~/.ssh/authorized_keys"
            }
        }
    ],
    "profile": "personal"
}
