{
    "version": "alpha",
    "schema": "core",
    "actions": [
        {
            "action": "script",
            "files": {
                "init": "~/.ssh/init"
            }
        },
        {
            "action": "symlink",
            "files": {
                "config.symlink": "~/.ssh/config",
                "authorized_keys.symlink": "~/.ssh/authorized_keys"
            }
        }
    ],
    "profile": "personal"
}
