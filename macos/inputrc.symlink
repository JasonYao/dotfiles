###
# This file contains configuration overrides for readline,
# which dictate behavior in the shell such as auto completion behavior
##

# Ignores case when doing auto completion
set completion-ignore-case On

# Only lists the auto-completions instead of ringing the bell when
#the completing word has more than one possible completion
set show-all-if-ambiguous on
